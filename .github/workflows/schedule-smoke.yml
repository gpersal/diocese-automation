name: schedule-smoke

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Print timestamps
        run: |
          echo "UTC now: $(date -u)"
          echo "Local now: $(date)"
